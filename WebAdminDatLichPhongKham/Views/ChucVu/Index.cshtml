@model IEnumerable<WebAdminDatLichPhongKham.Models.ChucVu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenchucvu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenchucvu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idChucVu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idChucVu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idChucVu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
